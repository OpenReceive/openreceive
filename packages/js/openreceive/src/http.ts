// Umbrella subpath: the full @openreceive/http surface, so an umbrella-only
// install can follow the scaffold wiring guide (host + handler + reconciler +
// notification listener + SQL repository) without extra installs.
export * from "@openreceive/http";
