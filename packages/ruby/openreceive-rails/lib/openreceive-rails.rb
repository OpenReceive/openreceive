# frozen_string_literal: true

# Conventional dashed entry point (`require "openreceive-rails"`). Delegates to the namespaced
# require tree so both `require "openreceive-rails"` and `require "openreceive/rails"` work.
require "openreceive/rails"
