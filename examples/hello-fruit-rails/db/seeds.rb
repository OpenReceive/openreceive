# frozen_string_literal: true

# Nothing to seed. This quickstart has no catalog: the checkout page creates a
# fresh $2.00 `Order` ad hoc via POST /orders (see OrdersController#create),
# and the engine-owned `openreceive_payments` rows are created per checkout
# attempt. `bin/rails db:prepare` is all the database setup the demo needs.
