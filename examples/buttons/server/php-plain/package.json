{
  "name": "@openreceive/example-buttons-php-plain",
  "version": "0.4.3",
  "private": true,
  "type": "module",
  "description": "Buy a Button on plain PHP. Not an npm workspace: it has no npm dependencies of its own — Vite, the shared vanilla client and the standalone checkout build all come from the repository root.",
  "scripts": {
    "dev": "node ../../../../tools/run-with-root-env.mjs vite --host 127.0.0.1 --port 3008 --configLoader runner",
    "build": "vite build --configLoader runner",
    "start": "node ../../../../tools/run-with-root-env.mjs php -S 0.0.0.0:3008 -t public public/index.php"
  }
}
