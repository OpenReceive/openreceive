# frozen_string_literal: true

source "https://rubygems.org"

gem "rails", "~> 8.1.3"
gem "propshaft"
# Webpack-driven frontend (React + MobX Keystone). Version pinned to match the
# npm shakapacker package exactly (ensure_consistent_versioning).
gem "shakapacker", "9.7.0"
# Database-backed ActionCable adapter: lets the out-of-process reconciler and
# openreceive:notifications workers broadcast settlement into Puma without Redis.
gem "solid_cable"
gem "pg", "~> 1.1"
gem "puma", ">= 5.0"
gem "tzinfo-data", platforms: %i[windows jruby]

# Path gems from this monorepo (Dockerfile copies packages/ruby into the image).
gem "openreceive", path: "../../../../packages/ruby/openreceive"
gem "openreceive-server", path: "../../../../packages/ruby/openreceive-server"
gem "openreceive-rails", path: "../../../../packages/ruby/openreceive-rails"
gem "nwc-ruby"
gem "dotenv-rails", groups: %i[development test]

group :development, :test do
  # bin/bundler-audit (run by bin/ci) needs the gem inside the app bundle.
  gem "bundler-audit", require: false
end

group :development do
  gem "web-console"
end
