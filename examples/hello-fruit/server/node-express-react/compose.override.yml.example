services:
  hello-fruit-node-express-react:
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]
    environment:
      NODE_ENV: development
    ports:
      - "3000:3000"
