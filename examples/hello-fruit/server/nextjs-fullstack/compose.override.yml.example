# Local port publishing for `npm run demo` / `make docker-run`.
# Ports only: the image's built production server is what the demo runs.
services:
  hello-fruit-nextjs-fullstack:
    ports:
      - "3002:3002"
